import React, { useEffect, useRef } from 'react';

interface ThreeEx { }

const ThreeEx: React.FC<ThreeEx> = (props) => {


    return (
        <>
            <canvas></canvas>
        </>
    );
};

export default ThreeEx;
